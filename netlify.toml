[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# analyze is a streaming Functions 2.0 endpoint — it bypasses the sync
# function timeout (Free's 10s cap). No timeout config needed.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
